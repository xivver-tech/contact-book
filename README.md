# Contact Book

Local contact manager with search, tags and CSV export.

## Usage
```bash
python contacts.py add
python contacts.py list
python contacts.py list --tag work
python contacts.py search alice
python contacts.py delete 3
python contacts.py export
```
